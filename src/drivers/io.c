#include "common.h"
#include <io.h>

κενός είσοδος(unsigned short πύλη, unsigned char δεδομένα) {
    πτητική_εντολή("outb %1, %0" :: "dN" (πύλη), "a" (δεδομένα));
}

θετικός χαρακτήρας έξοδος(unsigned short πύλη) {
    θετικός χαρακτήρας α;
    πτητική_εντολή("inb %1, %0" : "=a" (α) : "dN" (πύλη));
    επέστρεψε α;
}